document.addEventListener("DOMContentLoaded",function(){
  player = document.getElementsByTagName('input')
  for(var i = 0; i < player.length; i++){
    switch (player[i].id) {
      case expression:

        break;
      default:

    }
  }
});
